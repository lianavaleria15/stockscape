<header>
   {{#if loggedIn}}
  {{> private-navbar}}
  {{else}}
  {{> public-navbar}}
  {{/if}}
</header>
<main>
  <div id="companies-container">
  {{#each companies}}
  {{> company-card parent=../this}}
  {{/each}}</div>
</main>

