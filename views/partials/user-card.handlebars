<div class="card m-3" style="width: 18rem;">
  <div class="card-body">
    <h5 class="card-title">{{username}}</h5>
    <h6>Investor Type: {{investor_type}}</h6>
    <h6>Favourite Company: {{name}}</h6>
    <button class="btn btn-primary" name="view-profile-btn" id={{id}}>View
      Profile</button>
  </div>
</div>